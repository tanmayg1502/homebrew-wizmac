# homebrew-wizmac

Homebrew tap for Wizmac.

Install with:

```bash
brew tap tanmayg1502/wizmac
brew install --cask wizmac
```

This tap distributes the packaged application only. Public release assets are hosted in `tanmayg1502/wizmac-releases`.
